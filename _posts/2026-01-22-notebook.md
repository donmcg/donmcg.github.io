---
layout: post
title: donmcg.github.io notebook 
tip: Restarting the CAN prototype board
project: donmcg.github.io
author: donmc
date:   2026-01-22 01:00:00 -0700
---

# Notes for building donmc.github.io
Date of this post: {{ page.date | date: "%d %b %Y" }}

|[Published GitHub Site](https://donmcg.github.io) | [local site](http://localhost:4000) |


I am having success developing the
[Fairhillbillies SU Piston Position Sensor]({{ site.url | append: "/mini/project/su.piston.position/su.piston.position-fairhillbillies.html" }} )
and I'd like to begin sharing my work.

* I built a CAN prototype board some months ago and it's nearing
time to crank it up and see if I can get a SparkFun CAN-Bus shield to
work with it.

* Presented here is mostly revisiting my lab book dated late august 2025.

---
<br>


 22jan2026 dump of history from late august 2025 investigating CAN
 collected on studio1 (ubuntu studio)

{% highlight bash %}
studio1 $ history 
 1427  type can_logger
 1428  can_logger -h
 1429  type conda
 1430  history | grep can0
 1431  [200~sudo ip link set can0 up type can bitrate 125000 loopback on
 1432  sudo ip link set can0 up type can bitrate 125000 loopback on
 1433  candump can0
 1434  ip link show
 1435  sudo ip link set can0 up type can bitrate 125000 sample-point 0.875
 1436  sudo ip link set can0 down
 1437  ip link show
 1438  sudo ip link set can0 up type can bitrate 125000 sample-point 0.875
 1439  sudo ip link set can0 up
 1440  sudo ip link set can0 down
 1441  sudo ip link set can0 up type can bitrate 125000 loopback on
 1442  candump can0
 1443  sudo ip link set can0 up type can bitrate 125000 loopback off
 1444  sudo ip link set can0 down
 1445  sudo ip link set can0 
 1446  cansend can0 1f334455#1122334455667788
 1447  sudo ip link set can0 down
 1448  sudo ip link set can0 up
 1449  cansend can0 1f334455#1122334455667788
 1450  sudo ip link set can0 down
 1451  sudo ip link set can0 up
 1452  sudo ip link set can0 down
 1453  sudo ip link set can0 up
 1454  sudo ip link set can0 down
 1455  cansend can0 1f334455#1122334455667788
 1456  sudo ip link set can0 up
 1457  cansend can0 1f334455#1122334455667788
 1458  /show
 1459  ip link show
 1460  sudo ip link set can0 up
 1461  ip link show
 1462  sudo ip link set can0 up
 1463  ip link show
 1464  cansend can0 1f334455#1122334455667788
 1465  sudo ip link set can0 up type can bitrate 125000 sample-point 0.875
 1466  cansend can0 1f334455#1122334455667788
 1467  sudo ip link set can0 down
 1468  sudo ip link set can0 up
 1469  cansend can0 1f334455#1122334455667788
 1470  sudo ip link set can0 down
 1471  sudo ip link set can0 up
 1472  ip link show
 1473  cansend can0 1f334455#1122334455667788
 1474  candumb can0
 1475  candump can0
 1476  cansend can0 1f334455#1122334455667788
 1477  sudo ip link set can0 down
 1478  sudo ip link set can0 up type can bitrate 125000 loopback on
 1479  ip link show
 1480  cansend can0 1f334455#1122334455667788
 1481  sudo ip link set can0 up type can bitrate 125000 loopback off
 1482  sudo ip link set can0 down
 1483  sudo ip link set can0 up type can bitrate 125000 loopback off
 1484  cansend can0 1f334455#1122334455667788
 1485  sudo ip link set can0 down
 1486  sudo ip link set can0 up type can bitrate 125000 one-shot on
 1487  cansend can0 1f334455#1122334455667788
 1488  sudo ip link set can0 down
 1489  sudo ip link set can0 up type can bitrate 125000 sample-point 0.875
 1490  cansend can0 1f334455#1122334455667788
 1491  sudo ip link set can0 down
 1492  sudo ip link set can0 up type can bitrate 125000 sample-point 0.875
 1493  cansend can0 1f334455#1122334455667788
 1494  sudo ip link set can0 down
 1495  ip link show
 1496  sudo ip link set can0 down
 1497  sudo ip link set can0 up
 1498  sudo ip link set can0 down
 1499  sudo ip link set can0 up type can bitrate 125000 loopback on
 1500  cansend can0 1f334455#1122334455667788
 1501  sudo ip link set can0 type can help
 1502  sudo ip link set can0 down
 1503  sudo ip link set can0 up type can bitrate 125000 one-shot on
 1504  cansend can0 1f334455#1122334455667788
 1505  sudo ip link set can0 down
 1506  cansend can0 1f334455#1122334455667788
 1507  sudo ip link set can0 up type can bitrate 125000 one-shot on
 1508  cansend can0 1f334455#1122334455667788
 1509  sudo ip link set can0 down
 1510  sudo ip link set can0 up type can bitrate 500000 one-shot on
 1511  cansend can0 1f334455#1122334455667788
 1512  sudo ip link set can0 down
 1513  sudo ip link set can0 up type can bitrate 125000 one-shot on
 1514  cansend can0 1f334455#1122334455667788
 1515  sudo ip link set can0 down
 1516  sudo ip link set can0 type can bitrate 500000
 1517  cansend can0 1f334455#1122334455667788
 1518  sudo ip link set can0 down
 1519  sudo ip link set can0 up type can bitrate 125000 one-shot on
 1520  cansend can0 1f334455#1122334455667788
 1521  sudo ip link set can0 down
 1522  sudo ip link set can0 up type can bitrate 500000 one-shot on
 1523  cansend can0 1f334455#1122334455667788
 1524  sudo ip link set can0 up type can bitrate 500000 one-shot on
 1525  cansend can0 1f334455#1122334455667788
 1526  sudo ip link set can0 down
 1527  sudo ip link set can0 up type can bitrate 500000 listen-only
 1528  sudo ip link set can0 up type can bitrate 500000 listen-only on
 1529  sudo ip link set can0 down
 1530  sudo ip link set can0 up type can bitrate 500000 listen-only on
 1531  sudo ip link set can0 down
 1532  sudo ip link set can0 up type can bitrate 500000 loopback on
 1533  sudo ip link set can0 down
 1534  sudo ip link set can0 up type can bitrate 500000
 1535  candump can0
 1536  ip -d -s link show can0
 1537  sudo ip link set can0 down
 1538  sudo ip link set can0 up type can bitrate 500000
 1539  ip -d -s link show can0
 1540  sudo ip link set can0 down
 1541  sudo ip link set can0 up type can bitrate 500000 one-shot on
 1542  cansend can0 1f334455#1122334455667788
 1543  sudo ip link set can0 down
 1544  sudo ip link set can0 up type can bitrate 500000 one-shot on
 1545  cansend can0 1f334455#1122334455667788
 1546  sudo ip link set can0 up type can bitrate 500000 one-shot on
 1547  sudo ip link set can0 down
 1548  sudo ip link set can0 up
 1549  sudo ip link set can0 down
 1550  sudo ip link set can0 up
 1551  sudo ip link set can0 down
 1552  sudo ip link set can0 up type can bitrate x125000 sample-point 0.875
 1553  sudo ip link set can0 down
 1554  sudo ip link set can0 up type can bitrate 500000 sample-point 0.875
 1555  cansend can0 1f334455#1122334455667788
 1556  sudo ip link set can0 down
 1557  sudo ip link set can0 up
 1558  sudo ip link set can0 up type can bitrate 500000
 1559  cansend can0 1f334455#1122334455667788
 1560  ip -d -s link show can0
 1561  cansend can0 1f334455#1122334455667788
 1562  sudo ip link set can0 down
 1563  sudo ip link set can0 up type can bitrate 500000
 1564  cansend can0 1f334455#1122334455667788
 1565  sudo ip link set can0 down
 1566  sudo ip link set can0 up type can bitrate 500000
 1567  cansend can0 1f334455#1122334455667788
 1568  sudo ip link set can0 up type can bitrate 500000
 1569  sudo ip link set can0 down
 1570  sudo ip link set can0 up type can bitrate 500000
 1571  cansend can0 1f334455#1122334455667788
 1572  sudo ip link set can0 down
 1573  cansend can0 1f334455#1122334455667788
 1574  sudo ip link set can0 up type can bitrate 500000
 1575  cansend can0 1f334455#1122334455667788
 1576  sudo ip link set can0 down
 1577  sudo ip link set can0 up type can bitrate 500000
 1578  cansend can0 1f334455#1122334455667788
 1579  sudo ip link set can0 type can restart 
 1580  sudo ip link set can0  down
 1581  sudo ip link set can0 type can restart 
 1582  sudo ip link set can0 up type can bitrate 500000 restart
 1583  sudo ip link set can0 up type can restart bitrate 500000 
 1584  sudo ip link set can0 up type can restart-ms 100 bitrate 500000 
 1585  cansend can0 1f334455#1122334455667788
 1586  sudo ip link set can0 down
 1587  sudo ip link set can0 up type can restart-ms 100 bitrate 500000 
 1588  cansend can0 1f334455#1122334455667788
 1589  sudo ip link set can0 down
 1590  sudo ip link set can0 up type can restart-ms 1 bitrate 500000 
 1591  sudo ip link set can0 down
 1592  sudo ip link set can0 up type can restart-ms 100 bitrate 500000 
 1593  sudo ip link set can0 down
 1594  candump can0
 1595  lsusb
 1596  candump can0
 1597  man socketcan
 1598  man SocketCAN
 1599  ls
 1600  cd msi/project/CAN/
 1601  ls
 1602  mkdir socketcan
 1603  cd socketcan/
 1604  vi networking.can.txt
 1605  candump can0
 1606  man candump
 1607  candump can0
 1608  ip link show
 1609  candump can0
 1610  candump -h
 1611  can.logger -h
 1612  can_logger -h
 1613  can_logger -c can0 
 1614  can_logger -c can0 -i socketcan
 1615  candump can0
 1616  can_logger -c can0 -i socketcan
 1617  can_logger -c can0 -i socketcan -b 500000
 1618  candump can0
 1619  candump -h
 1620  candump -c -c -ta can0,0:0,#1F334455
 1621  candump -h
 1622  candump -c -c -ta can0,123:7FF,#1F334455
 1623  candump -h
 1624  candump -c -c -ta can0,#1F334455:7FF
 1625  candump -c can0
 1626  candump -c  can0,#1F334455:07FF
 1627  candump  can0,#1F334455:7FF
 1628  candump -c  can0,#1F334455:0FF
 1629  candump -c  can0,#1F334455:0
 1630  candump can0
 1631  candump can0 | grep 1F334455
 1632  candump -c can0 | grep 1F334455
 1633  candump -c can0 
 1634  candump -c can0 | grep 0000B601
 1635  candump -c can0,#1F334455
 1636  candump -c can0,#1F334455 | grep B601
 1637  candump -c can0,#1F334455 | grep B600
 1638  candump -c can0,#1F334455 | grep B602
 1639  candump -c can0,#1F334455 | grep B601
{% endhighlight %}


{% highlight bash %}
{% endhighlight %}


---

<br>

#### Some interesting links:

[]()


